language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "Twelve"
  - git config user.email "ebola52012@outlook.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${taken}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-source
env:
 global:
   - GH_REF: github.com:Twelvecat/Twelvecat.github.io.git
